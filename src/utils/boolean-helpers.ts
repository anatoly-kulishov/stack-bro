/**
 * Check that the theme color is "light"
 * @param appTheme
 */
export const isLightTheme = (appTheme: string) => (appTheme === 'light');

/**
 * Check that the theme color is "dark"
 * @param appTheme
 */
export const isDarkTheme = (appTheme: string) => (appTheme === 'dark');